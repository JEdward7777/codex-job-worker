[project]
name = "codex-job-launcher"
version = "0.1.0"
description = "SkyPilot launcher for codex-job-worker"
requires-python = ">=3.10"
dependencies = [
    "skypilot[vast]>=0.9.0",
]

[dependency-groups]
dev = []
